// 这是内容脚本，用于提取当前网页中的特定信息
document.addEventListener('DOMContentLoaded', () => {
    // 示例：提取网页标题
    const pageTitle = document.title;
    console.log('当前网页标题:', pageTitle);

    // 在这里添加更多提取特定信息的代码
});